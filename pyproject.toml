[project]
name = "lewm-seminar"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "marimo>=0.23.9",
    "bibtexparser>=1.4.0",
]

[project.optional-dependencies]
dev = [
    "manim>=0.20.1",
    "playwright>=1.60.0",
]
